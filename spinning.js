function spinWords(str){
 return str.split(" ").map((item,index) => item = [item]).map((item,index) => item = item[0].length > 4 ? item.join("").split("").reverse().join("").split(","): item).join(" ");
}
